import DefaultPageLayout from 'layouts/Default';
import React from 'react';

const Index: React.FC = (props) => {
    return <DefaultPageLayout pageClassName="page-main">Main page</DefaultPageLayout>;
};

export default Index;
